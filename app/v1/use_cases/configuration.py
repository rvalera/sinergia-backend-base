'''
Created on 17 dic. 2019

@author: ramon
'''
# from app.v1.repository.configuration import HolguraRepository,TurnoRepository

# class GetHolguraListUseCase(object):
#     def execute(self,security_credentials,query_params):
#         return HolguraRepository(username=security_credentials['username']).get(query_params)

# class NewHolguraUseCase(object):
#     def execute(self,security_credentials,payload):
#         HolguraRepository(username=security_credentials['username']).new(payload)

# class SaveHolguraUseCase(object):
#     def execute(self,security_credentials,payload):
#         HolguraRepository(username=security_credentials['username']).save(payload)   

# class DeleteHolguraUseCase(object):
#     def execute(self,security_credentials,query_params):
#         HolguraRepository(username=security_credentials['username']).delete(query_params)   

# class ApproveHolguraUseCase(object):
#     def execute(self,security_credentials,id):
#         HolguraRepository(username=security_credentials['username']).approve(id)   


# ###################################

# class GetTurnoListUseCase(object):
#     def execute(self,security_credentials,query_params):
#         return TurnoRepository(username=security_credentials['username']).get(query_params)

# class NewTurnoUseCase(object):
#     def execute(self,security_credentials,payload):
#         TurnoRepository(username=security_credentials['username']).new(payload)

# class SaveTurnoUseCase(object):
#     def execute(self,security_credentials,payload):
#         TurnoRepository(username=security_credentials['username']).save(payload)   

# class DeleteTurnoUseCase(object):
#     def execute(self,security_credentials,query_params):
#         TurnoRepository(username=security_credentials['username']).delete(query_params)           



             